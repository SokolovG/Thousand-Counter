name: thousand_counter
description: "Counter for card game Thousand"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  talker: ^5.1.10
  uuid: ^4.5.2
  recase: ^4.1.0
  flutter_riverpod: ^2.6.1
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
